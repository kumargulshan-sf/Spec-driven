# Work on 2026-07-22
_Automated daily summary._

## Commits

### afs-workspace/salesforcedx-vscode
  - `25e69b4c9` removed template from core _(2026-07-22, kumargulshan)_
  - `4d40cc0e1` fix: re-export ProjectOptions from services to fix dependency resolution _(2026-07-22, kumargulshan)_

## Pull Requests

### gitcore.soma.salesforce.com (kumargulshan)
  - [closed] @W-23153627: Added channel id in sfdc_env — core-2206/core-264-public
    https://gitcore.soma.salesforce.com/core-2206/core-264-public/pull/55986

## Email highlights
_No email activity captured._

## Slack highlights
- **#proj-sf-multi-framework** — Asked Brian Buchanan whether local namespace/appNameEntry changes tie into the bundleId build changes; pinged Tarushi that Angular (PR #789) needs the same changes; shared a related thread link.
- **#plugin-design-templates** — Explained the npm hoisting issue with `@salesforce/templates` in the metadata package (works today via root fallback, breaks on `npm i`); proposed exporting `ProjectOptions` from the services package instead of importing directly from templates; asked whether the root templates dep was added manually or via npm.
- **DM with Tarushi** — Debugged the dependency-resolution issue together: package-lock updates (revert vs keep), shared a patch importing `ProjectOptions` via services, confirmed no lock-file update needed post-approval, and coordinated a debugging call.

## Claude sessions
- Worked in **salesforcedx-vscode** on a dependency-resolution fix: the metadata package couldn't resolve `ProjectOptions` when imported directly from `@salesforce/templates`, so re-exported it from the services package instead.
- Removed the template dependency from core and validated with clean (no-cache) compiles across affected modules.
- Synced a forked branch against the upstream PR (#7678), then pushed to a new `afs/dep-resolution` branch and raised a PR from the `kumargulshan-sf/salesforcedx-vscode` fork.
- Pulled from develop and resolved merge conflicts (keeping incoming), and reviewed related upstream PRs (#7843).
- Drafted/rephrased a summary (based on Tarushi's post) explaining the metadata→services dependency-declaration problem for the team.
- Reset `npm config min-release-age` and made a patch as part of the workflow.

## Summary
The day centered on untangling a dependency-resolution problem in salesforcedx-vscode: the metadata package failed to resolve `ProjectOptions` when importing straight from `@salesforce/templates` (which only worked via an npm root-hoisting fallback that breaks on a fresh `npm i`), so the fix re-exports `ProjectOptions` from the services package and removes the template dependency from core. Gulshan validated the change with clean no-cache compiles, resolved develop merge conflicts, and raised a PR from his fork on a new `afs/dep-resolution` branch. Alongside the code work, he coordinated heavily on Slack and in a DM with Tarushi to explain the npm hoisting root cause and align on the fix, and touched the multi-framework/bundleId effort where the same change is needed for Angular (PR #789). A separate gitcore PR adding channel id to `sfdc_env` (W-23153627) was closed.
