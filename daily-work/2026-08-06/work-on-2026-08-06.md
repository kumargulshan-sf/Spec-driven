# Work on 2026-08-06
_Automated daily summary._

## Commits

### Spec-driven
  - `f9bbec9` docs(daily-work): work summary for 2026-08-05 _(2026-08-06, kumargulshan)_

## Pull Requests

### gitcore.soma.salesforce.com (kumargulshan)
  - [open] @W-23722580 : Added developerName check to handle case insensitivity — core-2206/core-264-public
    https://gitcore.soma.salesforce.com/core-2206/core-264-public/pull/103241

## Email highlights
_No email activity captured._

## Slack highlights
_No Slack activity captured._

## Claude sessions
- **SLDS vs Tailwind / Material** — spent much of the morning reasoning through whether SLDS is a superset/subset of Tailwind, whether Tailwind is needed at all alongside Angular Material, and concluding Tailwind is over-engineering for the design bundle.
- **Theming strategy (SLDS + Material)** — settled the architecture: consume SLDS styling hooks (`--slds-g-*`) in the global `styles.scss`/theme layer only, override Material system tokens (`--mat-sys-*`) there so components stay framework-agnostic and pick up new colors automatically.
- **Palette vs semantic hooks** — clarified the distinction; agreed to map only via SLDS semantic hooks (not raw palette steps), leaving palette use optional at the component's discretion.
- **Dark/light theming** — worked through how theme values flow into SLDS and how to flip themes.
- **Storybook + proving the seam** — built a Storybook for the ui-components to test theming, then proved the token seam end-to-end (e.g. driving the button's primary color from a random SLDS color through `--mat-sys-primary`).
- **`applying-slds` skill** — evaluated the internal skill, ran it, and noted its limitations (no framework-specific guidance, no theme-flip/hook coverage; hooks cover semantics only).
- **Spacing / layout classes** — mapped SLDS spacing tokens against Material spacing, deciding spacing lives at both component and global level, and SLDS layout classes can replace the removed Tailwind utility classes.
- **UI-bundle handler design** — late-day discussion on splitting the handler into two (one for ui-bundle, one for file-based) to avoid conditional checks, keeping a single handler reusable if the container grows.

## Summary
The day was almost entirely a design-and-strategy session on styling the Angular design bundle: resolving whether Tailwind is needed (no — Material theme plus SLDS is enough) and settling a clean theming architecture where SLDS styling hooks are consumed only in the global theme layer and remapped onto Material's `--mat-sys-*` tokens, keeping components framework-agnostic. Along the way the `applying-slds` skill was trialed and its limitations documented, spacing/layout-class strategy was decided, and a Storybook was stood up to prove the token seam. Concrete output landed as the automated daily-work doc commit in Spec-driven and an open case-insensitivity fix PR on core-264-public; email and Slack activity were not captured this run.
