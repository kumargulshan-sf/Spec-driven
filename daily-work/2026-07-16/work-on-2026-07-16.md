# Work on 2026-07-16
_Automated daily summary._

## Commits

### Spec-driven
  - `d0d73a5` docs(daily-work): work summary for 2026-07-15 _(2026-07-16, kumargulshan)_

### afs-workspace/webapps
  - `84e49c39a` feat(template): @W-23301688@ compose Angular features into b2x/b2e apps and publish to npm (#759) _(2026-07-16, Gulshan Kumar)_
  - `3af20f25c` docs(template): clarify routeFilePath comment in angularinternalapp patch _(2026-07-16, kumargulshan)_
  - `c4ab72a6c` feat(patches-cli): @W-23301688@ replace <%= bundlename %> in angular.json on compose (#760) _(2026-07-16, Gulshan Kumar)_

### afs-workspace/salesforcedx-templates
  - `36513ea` chore: bump Angular UI bundle templates to ^11.30.0 _(2026-07-16, kumargulshan)_

## Pull Requests

### github.com (kumargulshan_sfemu)
  - [merged] feat(patches-cli): @W-23301688@ replace <%= bundlename %> in angular.json on compose — salesforce-experience-platform-emu/webapps
    https://github.com/salesforce-experience-platform-emu/webapps/pull/760
  - [merged] feat(template): @W-23301688@ compose Angular features into b2x/b2e apps and publish to npm — salesforce-experience-platform-emu/webapps
    https://github.com/salesforce-experience-platform-emu/webapps/pull/759

## Email highlights
_No email activity captured._

## Slack highlights
- **DM w/ Tarushi Singla** — `sf pack:verify` enforces a 157-char Windows path limit (`node_modules/` paths); Angular template paths hit ~206 chars. Root cause: the path-shortening system (`features/` → `_f_`) looks for `features/` directly under `src/`, but Angular uses `src/app/features/...` so no shortening happens. Options discussed: drop the extra `app/` folder vs. add Angular-specific shortening in salesforcedx-templates. Agreed to send the PR (salesforcedx-templates #864) to the CLI team and ask about the strictness of the 157 limit.
- **DM w/ Tarushi Singla** — angular.json plugin config: move the API plugin to the `production` configuration and the codegen plugin to `development` (they were globally declared under `options`, so build plugins were also running on `ng serve`); goal is API plugin build-only, codegen serve-only.
- **DM w/ Tarushi Singla** — validated the end-to-end `sf project generate` flow: `--template angularexternalapp` (B2X/Experience) and `--template angularinternalapp` (B2E/CustomApplication) both fetch from npm and build a proper SFDX project; bumped base-angular-app dep to `^11.30.0`. Confirmed salesforcedx-templates + plugin-templates changes look good.
- **DM w/ Venkata Kolla** — pointed him to the team channel for a spike PR review (core-262-public #115250) and to tag app-fabric-service-dev; flagged that the page URL dev-name should be the ui-bundle developerName (else the Provider needs updating too).

## Claude sessions
- Resolved PR merge conflicts against `main` (webapps #759 and #760), pushed the fixes, and worked out the correct conventional-commit type (minor vs feat) plus merge comments for both PRs.
- Dug into the `angularinternalapp` patch — why a routefile was added and clarifying that the comment should point at React's `src/routes.tsx`.
- Wired up the publish flow: linking salesforcedx-templates and plugin-templates to the newly published Angular internal/external npm packages, updating project versions, and figuring out the proper `sf` project-generate command.
- Explored Web Components — using them with Vite/React, pros/cons, component lifecycle, and what JS/CSS they bring along.
- Started on Angular "design mode": read the Angular docs, investigated why `indexHtmlTransformer` wasn't used to inject scripts (build-time only), evaluated `@angular-builders/custom-esbuild`, and dug into angular.json build/serve configuration options.
- Planned a plugin refactor to replace the html.js dev-server middleware with `indexHtmlTransformer` on `ng serve`; created a `refactor/` folder and wrote up the plan doc.
- (Core repo) Reviewed the uibundle-packaging "second brain" to build a non-technical explanation of what packaging is and what's been done.

## Summary
Shipped the Angular publish milestone (W-23301688): merged webapps #759 (compose Angular features into b2x/b2e apps + publish to npm) and #760 (replace the `<%= bundlename %>` placeholder in angular.json on compose), resolving main conflicts along the way, and bumped the salesforcedx-templates deps to ^11.30.0. Coordinated closely with Tarushi on the downstream generator flow — validating `sf project generate` for both Angular external/internal templates and untangling the Windows 157-char `pack:verify` path-limit caused by Angular's extra `src/app/` folder defeating the path-shortening system. Also began the next chapter of work: research into Web Components and Angular "design mode", leading to a planned plugin refactor to swap the html.js middleware for `indexHtmlTransformer` on `ng serve`.
