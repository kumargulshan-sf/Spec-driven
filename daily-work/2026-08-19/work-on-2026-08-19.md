# Work on 2026-08-19
_Automated daily summary._

## Commits

### Spec-driven
  - `536acb1` docs(daily-work): work summary for 2026-08-18 _(2026-08-19, kumargulshan)_

## Pull Requests

### gitcore.soma.salesforce.com (kumargulshan)
  - [closed] @W-23896291: Added internal app check for app domain blocker — core-2206/core-264-public
    https://gitcore.soma.salesforce.com/core-2206/core-264-public/pull/116361

## Email highlights
_No email activity captured._ (Gmail/Google Workspace MCP not registered on this machine.)

## Slack highlights
- **#afs-india** — Govind Singh flagged W-23896291 as a likely DF blocker with many people reaching out; confirmed "we are on it," then reported the fix is found and a PR will follow after testing.
- **Rohit Kumar Sankaran (DM)** — Rohit offered to take over W-23896291 if I was busy with Angular; replied "checking" (no handoff — I kept it).
- **Mastan Mohammed (DM)** — debugged a React UIBundle site that redirects to the Salesforce login and won't render; site serves HTML but not JS/CSS assets, network shows 301, no console errors. Concluded it's a local-org URL/redirection issue; will retry on orgfarm.
- **Navateja Alagam (DM)** — helped debug React UIBundle on 262 vs 266: app renders on 266 but routes to salesforce.app without rendering on 262. Diagnosed app-domain (not lightning-domain) apps and that app domain isn't provisioned on 262; pointed to the app-domain provisioning channel.
- **Shailesh Pachbhai (DM)** — confirmed a reported bug (GUS a07EE00002XMi5OYAT) is the same issue; agreed to sync on a call.

## Claude sessions
- Deep-dived React UIBundle asset serving: HTML loads but JS/CSS never reach `LightningWebRuntimeServlet` — traced request routing through `SiteContainerAppRequestHandler`/`DefaultRequestHandlerManager`, app-container detection, and 301 redirects for `/` and assets.
- Worked the W-23896291 fix (internal-app check for the app-domain blocker): converted a lightning-domain URL to the app-domain form and checked `SalesforcePagesUddAccessChecks.orgHasSalesforcePagesDeploy()` for whether a gate is needed.
- Investigated how `salesforcedx-vscode-einstein-gpt` sources its React sample app and the internal/external split — and whether that distinction actually comes from the `sf project generate` / `@salesforce/templates` flow.
- Created a small page/app linked to a ui-bundle: hit `Invalid Target value 'SalesforcePage'` (valid: CustomApplication, Experience), deployed under `CustomApplication`, and located `getValidTargetValues`.
- Deploy troubleshooting: recovered a deploy stuck at "waiting for org to respond" (resume) despite org-side logs.

## Summary
The day centered on unblocking React UIBundle rendering on Salesforce sites — most of the effort went into tracing why site assets (JS/CSS) never reach the Lightning web runtime servlet while HTML does, spanning both a live debugging thread with Mastan and a deep request-routing investigation in Claude sessions. In parallel, I drove W-23896291 (a DF-priority app-domain blocker) to a fix and a PR, and helped Navateja debug the same app-domain-vs-lightning-domain rendering gap on 262. Supporting threads covered how the einstein-gpt bundle sources its sample app / internal-external split and correcting a ui-bundle deployment target (`CustomApplication`, not `SalesforcePage`).
