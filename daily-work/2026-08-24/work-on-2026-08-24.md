# Work on 2026-08-24
_Automated daily summary._

## Commits

### Spec-driven
  - `94e7c05` docs(daily-work): work summary for 2026-08-21 _(2026-08-24, kumargulshan)_

## Pull Requests

_No PRs found for 2026-08-24._

## Email highlights

_No email activity captured._

## Slack highlights

- **DM w/ Tarushi Singla** — Angular follow-up scope: no product enhancement asks yet, work is mostly refactoring + skills; only an epic, no doc.
- **DM w/ Tarushi Singla** — GUS triage: close W-23599625 and add demo message link in thread; keep W-23965646 at P3 so it isn't flagged.
- **DM w/ Tarushi Singla** — parity discussion on `.graphqlrc.yml` (project vs common/root location) and whether a WI is needed under the 266 epic; codebuilder issue with angular-plugin to discuss after standup.
- **Angular Testing group DM** (Mastan, Amrit, Tarushi) — test plan for Angular: reuse React scenarios + stealthreads E2E; deployment B2C/B2E, app access with data, guest/auth users. Flagged Mastan's report that Angular deploy/install is slower than React → asked him to file a bug.
- **Group DM w/ Amrit + Tarushi** — whether docs are needed for W-22992663 or the existing Angular Testing Guide is sufficient (fundamentals same).
- **DM w/ Pritesh Khandelwal** — helped with how the CSP error was fixed (shared reference link).
- **#afs-india** — shared an Angular-related Google doc link.

## Claude sessions

- Extended the `adk-eval` evaluation harness for the `experience-ui-bundle-project-generate` skill to add an Angular path alongside React — dataset/prompt JSON structure (combining instruction + prompt) and whether existing datasets need renaming.
- Stood up the local eval run environment: node 22, Einstein / LLM-gateway API keys, `.env` wiring, and worked through `npx adk-eval --init` failures (E404 registry proxy, 401 Unauthorized) and VS Code CLI setup.
- Iterated on Angular eval scores (composite ~78–84%) across many runs — tuned skill/reference files (angular.json uniqueness check, install-dependency step) and dug into the scoring rubric (60% per-file + 20% structural + 20% xfile coherence).
- Compared Angular vs React results; React scored higher thanks to a simpler, cleaner skill — used that as the target to close the gap.
- Explored gold-file constraints (can't modify gold files), the `flatten-project` script, and ran full React+Angular sweeps to ensure React didn't regress while lifting Angular.

## Summary

Monday was split between planning/triage in Slack and hands-on evaluation work. On the coordination side I scoped remaining Angular work with Tarushi (refactoring + skills, no new product asks), handled GUS triage (W-23599625 close, W-23965646 kept at P3), aligned the Angular testing plan with Mastan/Amrit, and surfaced a potential Angular deploy/install-perf bug. The bulk of focused time went into the `adk-eval` harness for the `experience-ui-bundle-project-generate` skill — adding an Angular path, fighting through env/auth setup, and iterating scoring runs to raise Angular's composite toward React's cleaner baseline without regressing React.
